# include<iostream>
using namespace std;
int main(){
    /*int n = sizeof(arr)/sizeof(arr[0]);
    struct node* root = NULL;
    int i=0;
    struct node* currnode = root;
    int currparent = 0;
    while(i<n){
        root=newnode(arr[i]);
        currnode->left = insert(root,arr[2*i+1]);
        currnode->right = insert(root,arr[2*i+2]);
        
        i++
    }*/
    return 0;
}